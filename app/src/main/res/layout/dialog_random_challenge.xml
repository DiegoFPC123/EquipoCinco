<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:paddingBottom="24dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/dialogContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:background="@drawable/bg_dialog_random"
        android:paddingHorizontal="24dp"
        android:paddingTop="50dp"
        android:paddingBottom="40dp"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/tvChallenge"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Este es un reto aleatorio de ejemplo" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <View
        android:id="@+id/vCircleBackground"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:background="@drawable/bg_circle_random"
        app:layout_constraintBottom_toTopOf="@id/dialogContainer"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/dialogContainer" />

    <ImageView
        android:id="@+id/ivPokemon"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:scaleType="centerInside"
        app:layout_constraintBottom_toBottomOf="@id/vCircleBackground"
        app:layout_constraintEnd_toEndOf="@id/vCircleBackground"
        app:layout_constraintStart_toStartOf="@id/vCircleBackground"
        app:layout_constraintTop_toTopOf="@id/vCircleBackground"
        tools:src="@tools:sample/avatars" />

    <Button
        android:id="@+id/btnClose"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/orange_pico"
        android:text="Cerrar"
        android:textColor="@color/white"
        android:textStyle="bold"
        app:cornerRadius="24dp"
        app:layout_constraintBottom_toBottomOf="@id/dialogContainer"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/dialogContainer" />

</androidx.constraintlayout.widget.ConstraintLayout>